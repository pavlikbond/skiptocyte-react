import React from "react";

const GoogleButton = ({ type }) => {
    return <button className="w-full bg-[#4c8bf5] text-white font-semibold ">{type} With Google</button>;
};

export default GoogleButton;
